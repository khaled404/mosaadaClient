import React, {FC} from 'react';
import {View, Text} from 'react-native';

const Form: FC<{input: any}> = ({input}) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default Form;
